spring:
  datasource:
    username: root
    password: Password_root123456
    url: jdbc:mysql://localhost:3306/news?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
logging:
  level:
    root: warn
    net.xi.news: info
  file:
    name: log/new-pro.log

server:
  port: 8081